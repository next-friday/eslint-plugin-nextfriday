---
"eslint-plugin-nextfriday": patch
---

Clean up `jsx-newline-between-elements`: remove unused `forbidNewline` message id (dead code, never reported), move the auto-fixable notice to the documented top-of-file format, and switch documentation code fences from `jsx` to `tsx`.
